package boxes;

class HeavyBox extends Box {
    int weight;

    // конструктор
    HeavyBox(int w, int h, int d, int m) {
        width = w;
        height = h;
        depth = d;
        weight = m;
    }
}