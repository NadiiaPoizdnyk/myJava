import org.junit.runner.RunWith;
import org.junit.runners.Suite;

@RunWith(Suite.class)
@Suite.SuiteClasses({
        TestEvenNumbers.class,
        TestOddNumbers.class,
        TestNotNumbers.class
})

public class TestSuite {
}
